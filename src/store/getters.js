export const headlineFontColor = state => state.config.headlineFontColor;
export const stateConfig = state => state.config;
export const state = state => state;
export const headlineFontColorValue = state =>
  state.config.headlineFontColor.color;
